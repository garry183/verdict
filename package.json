{
  "name": "cicd-verdict",
  "version": "0.1.0",
  "description": "Renders a verdict on every failing test — real bug, locator drift, or flaky (scored). An open-source, Playwright-native, CI-artifact-driven test triage layer.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "verdict": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "e2e:install": "playwright install chromium",
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "e2e:triage": "playwright test; node dist/cli.js triage test-results/results.json --html verdict-dashboard.html --suite e2e"
  },
  "keywords": [
    "playwright",
    "test-triage",
    "flakiness",
    "ci",
    "locator"
  ],
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0"
  }
}
